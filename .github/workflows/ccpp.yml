name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: release
      run: bash ./get-release.sh
    # - name: work
    #   run: sudo bash ./build.sh


